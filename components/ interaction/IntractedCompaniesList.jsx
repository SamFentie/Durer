import { StyleSheet, Text, View } from "react-native";
import React from "react";

const IntractedCompaniesList = () => {
  return (
    <View>
      <Text>IntractedCompaniesList</Text>
    </View>
  );
};

export default IntractedCompaniesList;

const styles = StyleSheet.create({});
